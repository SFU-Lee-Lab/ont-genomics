// Institutional config for PDS Wimmer machine
params {
    max_memory = '32.GB'
    max_cpus = 8
    max_time = '120.h'
}

process {
    clusterOptions = { "--account=def-sponsor00" }
    executor = 'slurm'
    scratch = false
}